# 09. Write-time Verification Layer 동작원리

이 문서는 Write-time Verification Layer가 **왜 필요하고**, **어떻게 작동하고**, **어떤 공격을 막는지**를 그림 위주로 설명합니다. 형식 명세는 `docs/02-아키텍처.md`를 보세요. 이 문서는 "처음 보는 사람도 이해할 수 있게" 씁니다.

## TL;DR

> AI가 쓴 코드는 동작은 맞는데 **왜 그렇게 썼는지**를 알 수 없어서 리뷰가 불가능합니다. 이 레이어는 AI가 **자기가 쓴 코드를 스스로 심문하게** 만들어서, 답변 못 하면 rewrite 하게 강제합니다. 결과는 코드가 아니라 **Q&A 트랜스크립트**가 리뷰 대상이 됩니다.

## 왜 이게 필요한가

사람 코드와 AI 코드의 차이를 바둑에 빗대면 이렇습니다.

```mermaid
flowchart LR
    A["인간 기보 학습<br/>AlphaGo"] -->|이해 가능| B["이세돌이 복기 가능"]
    C["Self-play 학습<br/>AlphaGo Zero"] -->|이해 불가| D["이세돌도 복기 못함"]

    E["사람이 쓴 코드"] -->|의도 추론 가능| F["리뷰 가능"]
    G["AI가 쓴 코드"] -->|의도 불명| H["리뷰 불가 = opacity"]

    style H fill:#ffcccc
    style D fill:#ffcccc
```

각 줄의 코드는 읽히지만, **왜 이렇게 설계했는지**를 재구성할 수 없습니다. 리뷰어가 "이 함수 왜 필요해?"라고 물어도 원 저자(AI)는 이미 사라졌고, 인간 리뷰어는 추측밖에 못 합니다.

**상용 AI 코드 리뷰 도구**(CodeRabbit, Greptile, cubic, qodo 등)는 PR **제출 후** 리뷰입니다. 이미 투명성을 잃은 코드를 뒤늦게 긁는 셈이죠. 이 레이어는 **write-time**, 즉 AI가 파일을 저장하는 순간에 개입해서 "왜 그렇게 썼는지 증명해봐"를 강제합니다.

## 전체 구조 (높은 수준)

```mermaid
flowchart TB
    subgraph Claude["Claude 세션 (저자 AI)"]
        A["Write/Edit 도구 호출"]
    end

    subgraph Hook["PostToolUse 훅 (&lt;100ms)"]
        B["verify-queue.sh<br/>메타데이터 append"]
    end

    subgraph Queue["~/.ai-symbiote/state/"]
        C["verify-queue.jsonl<br/>(append-only 로그)"]
    end

    subgraph Session["SessionStart 알림"]
        D["setup-check.sh<br/>N pending 표시"]
    end

    subgraph Verify["/verify 스킬 (사용자가 명시적 호출)"]
        E["Pre-flight<br/>typecheck/lint/test"]
        F["Cold-read Reviewer<br/>Codex, 3 roles"]
        G["V2c 랜덤 subset<br/>K=3 질문 선택"]
        H["저자 답변 생성"]
        I["V1 Basic +<br/>V2b LLM Judge"]
    end

    subgraph Artifact[".ai-symbiote/qa/&lt;date&gt;/"]
        J["&lt;sha&gt;.md<br/>Q&amp;A 트랜스크립트"]
    end

    A --> B --> C
    C -.pending 카운트.-> D
    D -.사용자 트리거.-> E
    E --> F --> G --> H --> I
    I -->|GENUINE| J
    I -->|GAMING_DETECTED| H

    style C fill:#fff3cd
    style J fill:#d4edda
    style I fill:#cfe2ff
```

핵심은 **시간 분리**입니다. 훅은 <100ms로 큐잉만 하고 바로 끝납니다. 실제 검증(~30초 × N)은 사용자가 `/verify`를 호출할 때 몰아서 합니다. Claude Code hook의 `timeout: 10s` 제약을 우회하는 핵심 설계 결정이고, 이를 **Option D**라고 부릅니다.

## 한 번의 동작을 순서대로 (sequence diagram)

```mermaid
sequenceDiagram
    actor User as 사용자
    participant CC as Claude Code
    participant Hook as verify-queue.sh
    participant Queue as verify-queue.jsonl
    participant Skill as /verify 스킬
    participant Codex as Codex (다른 벤더)
    participant QA as .ai-symbiote/qa/

    Note over CC: AI가 파일 수정
    CC->>Hook: PostToolUse(Write|Edit)
    Hook->>Queue: JSON append (<100ms)
    Hook-->>CC: {"continue":true}

    Note over User: 논리 단위 완료 후
    User->>Skill: /verify 호출
    Skill->>Queue: pending 읽기 + 필터
    Skill->>User: "N건, 예상 비용 $X. 진행?"
    User-->>Skill: Y

    loop 각 entry마다
        Skill->>Skill: Pre-flight (typecheck/lint)
        Skill->>Codex: cold-read reviewer 호출
        Codex-->>Skill: 질문 N=5~8개
        Skill->>Skill: V2c 랜덤 K=3 선택
        Skill->>CC: 저자(Claude)가 답변
        CC-->>Skill: 답변 K=3
        Skill->>Skill: V1 basic 체크
        Skill->>Codex: V2b judge 판정
        alt GENUINE
            Codex-->>Skill: PASS
            Skill->>QA: artifact 저장
        else GAMING_DETECTED
            Codex-->>Skill: FAIL
            Skill->>CC: rewrite 지시
        end
    end

    Skill-->>User: 결과 요약 (PASS N / FAIL N)
```

### 왜 Codex를 reviewer와 judge로 쓰나?

**AI끼리의 담합(collusion)을 막기 위해서**입니다.

```mermaid
flowchart LR
    A["Claude Opus<br/>저자"] -.->|같은 벤더| B["Claude Opus<br/>리뷰어"]
    B -->|서로 비슷한 blind spot| C["둘 다 놓침"]

    D["Claude Opus<br/>저자"] ==>|다른 벤더| E["Codex<br/>리뷰어"]
    E ==>|훈련 분포 다름| F["놓치는 영역이 겹치지 않음"]

    style C fill:#ffcccc
    style F fill:#d4edda
```

arxiv 2602.16741 연구 결과: "LLM 코드 리뷰어는 훈련 분포가 다를수록 blind spot 공유가 적다." 이 레이어는 이 원칙을 **config override 불가능한 하드 룰**로 고정했습니다. Claude 저자의 코드는 반드시 Codex가 리뷰하고 판정합니다.

## Validator 2층 구조

V1과 V2는 역할이 다릅니다.

```mermaid
flowchart TB
    subgraph V1["V1 — 기본 스캐너 (필요조건, 충분조건 아님)"]
        V1a["V1a 길이 ≥ 50자"]
        V1b["V1b 코드 참조 패턴<br/>file.ext:line"]
        V1c["V1c '모르겠음' 키워드 부재"]
        V1d["V1d 대안 언급 1개 이상"]
    end

    subgraph V2["V2 — Gaming 저항 (필수 방어선)"]
        V2a["V2a PR 본문 ↔ 답변<br/>bigram overlap &gt; 40%"]
        V2b["V2b LLM Judge<br/>Verifiability check"]
        V2c["V2c 질문 랜덤화<br/>N=5~8 → K=3"]
    end

    Answer["저자 답변"] --> V1a
    V1a --> V1b --> V1c --> V1d
    V1d -->|통과| V2a
    V1d -->|실패| Rewrite["즉시 rewrite"]
    V2a --> V2b
    V2b -->|GENUINE| Pass["✅ PASS"]
    V2b -->|GAMING_DETECTED| Rewrite
    V2c -.pool에서 미리 적용.-> V1a

    style Pass fill:#d4edda
    style Rewrite fill:#ffcccc
    style V2b fill:#cfe2ff
```

### V1만으로는 왜 부족한가

2026-04-20 Stage 0 실측에서 **V1만으로 3/3 gaming case 전부 통과**했습니다. 어떻게 뚫렸는지 예시:

**공격 시나리오**: 저자가 opaque한 코드를 쓰고, PR 본문에 그럴듯한 커버 스토리를 미리 심습니다. 질문이 오면 PR 본문의 표현을 paraphrase해서 답변.

| 답변 | V1a 길이 | V1b 코드 참조 | V1c 키워드 | V1d 대안 | V1 판정 |
|------|---------|-------------|----------|---------|--------|
| "`cache.ts:42`의 LRU를 대신 쓸 수도 있었지만, 우리 워크로드는 read-heavy라 write amortization이 더 중요해서 TinyLFU로 했습니다" | ≥50 ✓ | ✓ | ✓ | ✓ | **PASS** |

그런데 실제로 코드에 TinyLFU 같은 건 없고 그냥 평범한 Map이라면? V1은 못 잡습니다. **V2b judge**가 "그 주장을 뒷받침하는 artifact(벤치마크 파일, 주석, 참조 데이터)가 코드에 실재하는가?"를 구조적으로 검사해야 잡힙니다.

### V2c 질문 랜덤화가 필요한 이유

V2b judge만 있어도 seeding 공격의 **사전 준비 비용**이 낮습니다. 저자가 예상 질문 전부에 답변을 미리 준비할 수 있기 때문입니다.

```mermaid
flowchart LR
    subgraph NoV2c["V2c 없음"]
        A1["Reviewer가 5개 질문 생성"] --> A2["저자는 5개 전부 예측 가능"]
        A2 --> A3["5개 답변 모두 사전 준비"]
        A3 --> A4["judge 통과 시도"]
    end

    subgraph WithV2c["V2c 있음"]
        B1["Reviewer가 5~8개 풀 생성"] --> B2["시스템이 3개를<br/>무작위 선택"]
        B2 --> B3["저자는 어느 3개인지 모름"]
        B3 --> B4["모든 경우의 수에<br/>답변 준비 = 비용 폭증"]
    end

    style A4 fill:#ffcccc
    style B4 fill:#d4edda
```

## 배치 캡: 비용 폭주 방지

`verify-queue.jsonl`이 하루에 100건 쌓일 수도 있습니다. Codex judge 1회가 ~$0.15라면 100건 = $15. 실수로 Y 한 번 누르면 터집니다.

```mermaid
flowchart TD
    A["/verify 실행"] --> B{"pending N&gt;10?"}
    B -->|아니오| C["전체 N건 처리"]
    B -->|예| D["기본 10건만 처리"]
    D --> E["나머지는 queue에 유지"]
    E --> F["사용자가 다음 배치로<br/>재호출"]
    C --> G["완료"]
    F --> A

    H["--max-batch n으로<br/>기본값 override"] -.-> B

    style D fill:#fff3cd
```

## Pre-flight: reviewer에게 힌트 주기

기계검증(typecheck/lint/test)은 AI-generated 코드의 전형적 opacity 패턴을 값싸게 잡습니다. 이걸 reviewer 질문 생성 **전에** 미리 돌려서 결과를 프롬프트에 주입합니다.

```mermaid
flowchart LR
    A["diff 수집"] --> B["프로젝트 감지<br/>(tsconfig? Cargo.toml? go.mod?)"]
    B --> C["해당 검증 명령 실행<br/>(30초 timeout)"]
    C --> D["결과 앞 30줄 캡처"]
    D --> E["reviewer 프롬프트의<br/>INPUT 섹션에 주입"]
    E --> F["reviewer가 더 날카로운<br/>질문 생성"]

    style F fill:#d4edda
```

예시: lint가 `src/foo.ts:42 unused import`를 뱉으면, reviewer가 자동으로 "foo.ts:42의 unused import는 의도한 것인가, 아니면 이전 설계의 잔재인가?"를 묻게 됩니다. 이건 저자가 답변에서 진짜 이유를 드러내게 강제하는 좋은 프롬프트가 됩니다.

**Hard gate는 아닙니다**. 기계검증이 실패해도 Q&A는 계속 진행합니다. 목적은 reviewer에게 opacity 힌트를 주는 것이지 차단이 아닙니다.

## Artifact: 리뷰의 주체가 바뀐다

결과는 `.ai-symbiote/qa/<project>/<date>/<sha>.md`에 저장됩니다. 이 파일이 **인간 리뷰어의 실제 리뷰 대상**입니다.

```mermaid
flowchart LR
    A["코드 diff"] -.증거 자료.-> C
    B["Q&amp;A 트랜스크립트<br/>(pre-flight + 질문 + 답변<br/>+ V2b judge 판정)"] ==주된 리뷰 대상==> C["인간 리뷰어"]

    style B fill:#cfe2ff
    style A fill:#e9ecef
```

왜 PR 본문이 아니라 git-tracked 파일에 저장할까요?

- **PR 본문**: squash 가능, 덮어쓰기 가능 → 변조 위험
- **commit trailer**: 불변이지만 긴 transcript에 부적합
- **별도 저장소 (선택됨)**: git에 커밋되어 영속, PR 본문에 경로를 링크로 참조

## 플랫폼별 지원

```mermaid
flowchart TB
    subgraph Claude["Claude Code ✅"]
        C1["hooks.json에 Write|Edit matcher"]
        C2["PostToolUse 자동 큐잉"]
        C3["/verify 스킬 사용 가능"]
    end

    subgraph Codex["Codex CLI ⚠️"]
        X1["hooks.json에 PostToolUse(Write|Edit) 없음"]
        X2["자동 큐잉 불가"]
        X3["/verify --sha 수동 호출만 가능"]
    end

    subgraph Cursor["Cursor ⏳"]
        S1["hooks 이벤트 규약 상이"]
        S2["v0.11+ 검토 예정"]
    end

    style Claude fill:#d4edda
    style Codex fill:#fff3cd
    style Cursor fill:#f8d7da
```

Codex 유저는 훅이 없는 대신 `/verify --sha <sha>` 또는 `--file <path>`로 수동 호출합니다. `build-codex.sh`가 `shared/skills/verify/SKILL.md`를 `dist/codex-symbiote/`로 복사하므로 스킬 자체는 노출됩니다.

## 상태 머신: entry 한 건의 일생

```mermaid
stateDiagram-v2
    [*] --> Queued: Write/Edit 발생
    Queued --> Processing: /verify 호출
    Processing --> PreFlightDone: typecheck/lint/test 실행
    PreFlightDone --> Questioning: reviewer 호출
    Questioning --> SubsetSelected: V2c K=3 선택
    SubsetSelected --> Answering: 저자 답변 생성
    Answering --> V1Check: V1 basic 검증
    V1Check --> Answering: V1 fail (답변만 재생성)
    V1Check --> JudgeCheck: V1 pass
    JudgeCheck --> Rewrite: GAMING_DETECTED
    JudgeCheck --> Saved: GENUINE
    Rewrite --> Questioning: 저자 리팩터
    Saved --> [*]: queue에서 제거, artifact 커밋
```

## 사용자가 실제로 보는 것

```mermaid
sequenceDiagram
    actor U as 사용자
    participant CC as Claude Code

    Note over CC: (AI가 여러 파일 수정 중)

    U->>CC: 새 세션 시작
    CC-->>U: "[Verify] 4 pending verification(s)<br/>for myproj/feat/xyz.<br/>Run /verify to process."

    Note over U: 논리 단위 완료했다 판단

    U->>CC: /verify
    CC-->>U: "4 pending. Estimated ~60s × 4 = ~240s,<br/>~$0.60 USD. Proceed? [Y/n]"
    U->>CC: Y

    Note over CC: pre-flight + reviewer + judge...

    CC-->>U: "Completed 4 verifications:<br/>PASS: 3  FAIL: 1<br/>FAIL artifacts:<br/>.ai-symbiote/qa/myproj/2026-04-21/def456.md<br/>Next: review and refactor per feedback."
```

## FAQ

**Q. 왜 자동으로 실행 안 하고 사용자가 `/verify`를 호출해야 하나요?**
A. 훅의 `timeout: 10s` 제약 때문에 judge(~30s)를 훅 안에 못 넣습니다. 또한 edit 하나하나마다 돌리면 흐름을 방해합니다. **논리 단위가 완성된 시점**에 몰아서 하는 게 맞습니다.

**Q. "모르겠음"이라고 답하면 어떻게 되나요?**
A. V1c가 키워드를 잡아서 즉시 rewrite를 트리거합니다. 단 이건 **옳은 반응**입니다. 답변 못 하는 코드는 opacity 확정이고, 그럼 리팩터해서 명확하게 만들어야 합니다. "모르겠음"이 rewrite 시그널이 되는 건 설계의 핵심 메커니즘입니다.

**Q. pre-flight 기계검증이 느린 프로젝트면 어쩌죠?**
A. 각 명령마다 30초 timeout입니다. 초과하면 `(pre-flight: timeout)`으로 기록하고 Q&A는 계속 진행합니다. pre-flight는 hard gate가 아닙니다.

**Q. V2c의 K=3이 너무 적어서 중요한 질문을 놓치면요?**
A. Reviewer의 3개 role(Intent/Structural/Alternative)은 pool N=5~8에서 **각각의 관점**을 대표합니다. K=3을 뽑을 때 role balance를 최대한 유지합니다. 전체를 답변하려면 `--all-questions` 플래그가 있지만 artifact에 `v2c_bypassed: true`가 각인되어 production 검증으로 인정되지 않습니다.

**Q. queue가 계속 쌓이면 어떻게 관리하나요?**
A. PASS 처리된 entry는 자동 삭제됩니다. FAIL이나 사용자가 `/verify`를 안 부르는 경우에만 누적됩니다. 향후 `gc` 스킬 확장으로 30일 이상 된 entry 정리 예정입니다.

**Q. 편집 후 커밋을 이미 했는데 `/verify`를 돌리면 뭘 보나요?**
A. 큐에 저장된 `base_sha`(편집 직전 HEAD)를 기준으로 diff를 수집합니다. 편집 후 새 커밋이 여러 개 있으면 `git diff <base_sha>..HEAD -- <file>`로 누적 변경을 한 번에 봅니다. `git show <base_sha>`처럼 이전 커밋을 보는 식의 실수는 Codex adversarial review(Finding 1)에서 잡혀 수정됐습니다.

**Q. `~/work/app`과 `~/tmp/app` 둘 다 쓰면 queue가 섞이나요?**
A. 섞이지 않습니다. v2 schema가 `repo_root` 절대경로 필드를 따로 저장해서 `/verify`와 SessionStart 알림 둘 다 `repo_root`를 1차 필터로 씁니다. `project`(basename)은 사람이 읽을 용도로만 남아 있습니다.

## 관련 파일

| 역할 | 위치 |
|------|------|
| 훅 스크립트 | `shared/hooks/scripts/verify-queue.sh` |
| SessionStart 알림 | `shared/hooks/scripts/setup-check.sh` (365-383줄) |
| 스킬 정의 | `shared/skills/verify/SKILL.md` |
| 훅 등록 | `platforms/claude/overlay/hooks/hooks.json` (PostToolUse Write\|Edit) |
| 형식 명세 | `docs/02-아키텍처.md` ("Write-time Verification Layer" 섹션) |
| 설계 문서 | `~/.gstack/projects/Jimmy-Jung-ai-symbiote/jimmy-main-design-20260420-225015.md` |
| 훅 단위 테스트 | `tests/test-verify-queue.sh` (22 케이스) |
| 알림 테스트 | `tests/test-setup-check-verify.sh` (9 케이스) |
